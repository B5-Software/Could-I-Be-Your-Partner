/*
 * SPDX-License-Identifier: GPL-3.0-or-later
 * Copyright (c) 2026 B5-Software
 *
 * This file is part of Could I Be Your Partner.
 *
 * Bundled skills — built-in skill definitions shipped with the app.
 * Each bundled skill is a JSON object with the same shape as a user skill:
 *   { id, name, description, prompt, scripts?, bundled: true }
 *
 * The `prompt` field is injected into the system context when the skill is
 * activated via the `activateSkill` tool. Most bundled skills are
 * prompt-only (no scripts) — they guide the agent's behavior rather than
 * execute code directly.
 *
 * Inspired by claude-code-ref/src/skills/bundled/*.
 */

'use strict';

const BUNDLED_SKILLS = [
  {
    id: 'bundled-debug',
    name: 'debug',
    description: '调试日志诊断：启用应用调试日志，读取最近事件，引导定位问题。适用于应用卡顿、报错、行为异常等场景。',
    bundled: true,
    allowedTools: ['readFile', 'listDirectory', 'localSearch', 'runTerminalCommand', 'getSystemInfo'],
    prompt: `# Debug — 调试日志诊断

你现在进入"调试诊断"模式。你的任务是协助用户定位应用问题。

## 诊断步骤

### Step 1 — 收集症状
询问用户（或从对话上下文提取）：
- 问题现象是什么？（报错信息、异常行为、卡顿）
- 何时开始出现？（具体时间或操作）
- 是否可复现？（每次都发生还是偶发）
- 最近做了什么变更？（安装、更新、配置修改）

### Step 2 — 检查应用日志
1. 调用 getSystemInfo 获取系统状态
2. 检查工作目录下是否有 .log 文件（通过 listDirectory）
3. 若有日志文件，调用 readFile 读取最近 100 行
4. 用 localSearch 在日志中查找 [ERROR]、[WARN]、Exception、Failed 等关键词

### Step 3 — 检查环境
1. 调用 getNetworkStatus 检查网络连通性（若问题与网络相关）
2. 通过 runTerminalCommand 检查相关进程状态（如适用）
3. 检查磁盘空间、内存使用（通过 getSystemInfo）

### Step 4 — 定位根因
基于收集的信息：
1. 列出最可能的 3 个根因（按可能性排序）
2. 对每个根因给出验证方法
3. 提供修复建议（优先级：立即可执行 > 需要配置 > 需要重启）

### Step 5 — 验证修复
1. 让用户尝试最可能的修复
2. 若未解决，回到 Step 2 收集更多信息
3. 记录已尝试的方案，避免重复

## 输出格式

诊断报告应包含：
- **症状摘要**：一句话描述问题
- **根因分析**：3 个候选根因及验证方法
- **建议修复**：分优先级列出
- **后续跟踪**：若问题复现应收集什么信息

## 约束
- 不要假设根因，必须基于日志/证据
- 若日志不足以下结论，明确告知用户需要更多信息
- 危险操作（修改系统文件、强制结束进程）必须先征得用户同意`
  },

  {
    id: 'bundled-loop',
    name: 'loop',
    description: '周期任务调度：把当前任务注册为 cron 周期执行。支持 Ns/Nm/Nh/Nd 间隔格式。',
    bundled: true,
    prompt: `# Loop — 周期任务调度

你的任务是把用户描述的周期性任务注册为 cron 调度。

## 解析规则

用户输入可能包含间隔信息，按以下规则解析：
1. **前导 token**：若用户输入以数字+单位开头（如 "30m 检查邮件"），提取间隔
2. **尾部从句**：若输入包含 "every N<unit>"（如 "检查邮件 every 30m"），提取间隔
3. **默认值**：若未指定间隔，使用 10m（10 分钟）

支持的单位：
- Ns — 秒（最小 600s，即 10 分钟）
- Nm — 分钟（最小 10m）
- Nh — 小时
- Nd — 天

## 调度流程

1. 从用户输入中解析任务描述与间隔
2. 将间隔转为 cron 表达式：
   - "10m" → "*/10 * * * *"
   - "1h" → "0 * * * *"
   - "1d" → "0 0 * * *"
3. 调用 Schedule 工具（action: "create"）创建周期任务
4. 立即触发一次执行（action: "trigger"）让用户看到首次输出

## 输出要求

- 创建后报告：任务名称、cron 表达式、下次执行时间（用北京时间表达）
- 提醒用户可用 action: "list" 查看所有周期任务
- 提醒用户可用 action: "pause"/"resume"/"delete" 管理

## 约束
- 周期任务的最小间隔为 10 分钟（系统限制）
- 任务 message 字段必须包含完整上下文（执行时无用户在场）
- 不要把一次性任务注册为周期任务`
  },

  {
    id: 'bundled-batch',
    name: 'batch',
    description: '并行子代理编排：把大规模迁移/重构任务分解为多个独立单元，spawn 子代理并行工作。',
    bundled: true,
    prompt: `# Batch — 并行子代理编排

你的任务是把大规模迁移/重构任务分解为多个独立单元，使用 runSubAgent 工具并行执行。

## 分解原则

1. **独立性**：每个单元应能独立完成，不依赖其他单元的中间产物
2. **粒度**：5-30 个单元为宜；过少失去并行价值，过多难以管理
3. **可验证**：每个单元应有明确的完成标准（测试通过、文件生成等）

## 执行流程

### Phase 1 — 任务分析
1. 与用户确认任务范围与目标
2. 识别可并行化的维度（按文件、按模块、按功能）
3. 估算每个单元的工作量

### Phase 2 — 分解
1. 把任务切分为 N 个独立单元
2. 为每个单元编写清晰的指令（包含目标、约束、验收标准）
3. 向用户展示分解方案，征得同意

### Phase 3 — 并行执行
1. 对每个单元调用 runSubAgent，传入：
   - task：单元的完整指令
   - tools：该单元允许使用的工具白名单
2. 收集所有子代理的结果
3. 汇总成功/失败统计

### Phase 4 — 验收与汇总
1. 检查每个子代理的输出是否符合验收标准
2. 对失败的单元分析原因，决定重试或人工介入
3. 向用户提交汇总报告

## 输出要求

- 分解方案：单元列表 + 各单元指令
- 执行进度：实时报告每个子代理的启动/完成
- 汇总报告：成功数/失败数/总耗时/关键产物

## 约束
- 子代理之间不能共享运行时状态（独立上下文）
- 危险操作（删除、覆盖）必须由主代理审批，不能下放给子代理
- 单个单元失败不应阻塞其他单元`
  },

  {
    id: 'bundled-remember',
    name: 'remember',
    description: '记忆审查：扫描 project_memory.md 与 user_profile.md，提议条目的提升/清理/冲突解决，不直接修改。',
    bundled: true,
    allowedTools: ['readFile', 'listDirectory', 'localSearch'],
    prompt: `# Remember — 记忆审查

你的任务是审查用户的记忆文件，生成结构化的变更提议报告。不要直接修改文件——所有变更必须先经用户批准。

## 审查步骤

### Step 1 — 收集所有记忆层
1. 调用 readFile 读取项目根目录的 project_memory.md（若存在）
2. 调用 readFile 读取 user_profile.md（若存在）
3. 若有 session 级别的 topics.md 或 session_memory_*.jsonl，扫描最近 2 天的条目
4. 记录每一层有哪些内容段落

### Step 2 — 为每条记忆条目分类
对 project_memory.md 中的每个条目，判断最佳归属：

| 归属 | 适合的内容 | 例子 |
|---|---|---|
| **user_profile.md** | 跨项目通用的用户偏好与背景 | "偏好简洁回复"、"技术栈为 Electron+Vue" |
| **project_memory.md** | 当前项目的规则、约束、约定 | "设置必须原子化保存"、"Code 模式使用独立工作区" |
| **保留原位** | 工作笔记、临时上下文、不确定的模式 | 会话级观察、待确认的规律 |

判断要点：
- 用户对外部工具的偏好（编辑器主题、IDE 快捷键）不适合放进记忆文件
- 工作流实践（PR 约定、合并策略）需确认是个人还是团队级
- 不确定时询问用户，不要猜测

### Step 3 — 识别清理机会
跨层扫描：
- **重复**：project_memory.md 中已有但 user_profile.md 也重复的条目 → 提议删除其中之一
- **过时**：被较新条目推翻的旧条目 → 提议更新或删除
- **冲突**：两层之间互相矛盾的条目 → 提议解决方案，标注哪个更近

### Step 4 — 生成报告
输出结构化报告，按操作类型分组：
1. **提升** — 需要移动的条目，含目标位置与理由
2. **清理** — 重复、过时、冲突的条目
3. **待确认** — 归属不明确，需要用户输入
4. **无需操作** — 应保留原位的条目简述

若记忆文件为空，明确告知并提议审查 project_memory.md 做清理。

## 约束
- 所有提议必须在修改前先呈现给用户
- 未经用户明确批准，不要修改任何文件
- 不要创建新文件，除非目标文件不存在
- 对模糊条目要询问，不要猜测`
  },

  {
    id: 'bundled-stuck',
    name: 'stuck',
    description: '卡住求助：当反复失败或无进展时，系统化地诊断卡点并提出替代方案。',
    bundled: true,
    prompt: `# Stuck — 卡住求助

当你（或用户）在任务中卡住时启用本模式：连续 2 次失败、循环重复同样操作、或明确表达"卡住了"。

## 诊断流程

### Step 1 — 暂停并承认
明确告知用户："我们似乎卡住了。让我换个角度分析。"
不要再重复尝试相同的方案。

### Step 2 — 重新评估问题
1. **回到原始目标**：用户真正想要的结果是什么？当前路径是否仍然是最优的？
2. **检查假设**：列出当前方案依赖的假设，逐个质疑
3. **识别卡点类型**：
   - **信息不足**：缺少必要的上下文或数据
   - **工具限制**：当前工具无法完成某步
   - **理解偏差**：对需求的理解可能不准确
   - **外部依赖**：等待外部条件（网络、权限、第三方服务）
   - **复杂度过高**：一次性试图解决太多

### Step 3 — 生成替代方案
提出至少 3 个不同的替代方向：
1. **绕过**：跳过卡点，用其他方式达成目标
2. **拆分**：把当前步骤拆成更小的子步骤
3. **回退**：回到上一个已知良好的状态，换路径
4. **求助**：明确告知用户需要什么信息或决策

### Step 4 — 推荐与执行
1. 按可行性排序替代方案
2. 推荐最优方案，说明理由
3. 询问用户是否采纳，或选择其他方案
4. 获得确认后执行

## 输出格式

卡点诊断应包含：
- **卡点描述**：一句话说明当前困境
- **根因分析**：卡点类型与具体原因
- **替代方案**：3 个不同方向，各含优劣
- **推荐方案**：首选方案与理由

## 约束
- 不要重复已失败的方案
- 不要假装进展——诚实承认卡住
- 每个替代方案必须是真正不同的路径，不是换皮的重试`
  },

  {
    id: 'bundled-ultracode',
    name: 'ultracode',
    description: '超级代码：深度思考模式，先规划再执行，适用于复杂架构、多模块改动、疑难算法。',
    bundled: true,
    prompt: `# Ultracode — 深度思考模式

进入深度思考模式。在动手之前，先充分理解、规划、评估风险。

## 启用条件
当任务满足以下任一条件时启用：
- 涉及 3 个以上文件的改动
- 架构层面的调整（数据流、模块边界、状态管理）
- 疑难算法或性能优化
- 跨多层的改动（前端+后端+IPC）
- 用户明确要求"仔细想想"或"深度分析"

## 执行流程

### Phase 1 — 理解（Understand）
1. **复述需求**：用自己的话复述用户的目标，确认理解无误
2. **约束识别**：列出技术约束、时间约束、兼容性约束
3. **影响范围**：调用 localSearch / listDirectory 识别受影响的文件与模块
4. **依赖分析**：识别上下游依赖，标注可能的连锁影响

### Phase 2 — 规划（Plan）
1. **方案设计**：提出 2-3 个候选方案
2. **权衡分析**：对每个方案列出优劣、风险、复杂度
3. **选择推荐**：推荐最优方案，说明理由
4. **步骤拆解**：把推荐方案拆成可验证的步骤（每步可独立验证）
5. **回滚预案**：为每步设计回滚方法

### Phase 3 — 评审（Review）
1. **边界检查**：确认方案不会破坏现有功能
2. **性能评估**：是否引入性能回退
3. **安全检查**：是否引入安全风险（注入、XSS、权限提升）
4. **用户确认**：向用户呈现完整方案，征得同意后再执行

### Phase 4 — 执行（Execute）
1. 按拆解的步骤逐步执行
2. 每步完成后立即验证（运行测试、检查输出）
3. 遇到偏差及时调整，必要时回到 Phase 2 重新规划
4. 全部完成后做整体验收

### Phase 5 — 总结（Summarize）
1. 列出实际改动
2. 标注潜在风险点
3. 建议后续跟踪事项

## 输出要求
- 每个阶段输出要清晰标注（【理解】【规划】【评审】【执行】【总结】）
- 不要跳过阶段，即使看起来简单
- 规划阶段的方案必须真正不同，不是换皮

## 约束
- 不动手之前不假设——用工具验证
- 不引入未被要求的"改进"
- 每个改动都要能解释"为什么"`
  },

  {
    id: 'bundled-simplify',
    name: 'simplify',
    description: '简化：在不改变行为的前提下减少代码复杂度，移除冗余、合并重复、提升可读性。',
    bundled: true,
    allowedTools: ['readFile', 'listDirectory', 'localSearch', 'editFile', 'multiEditFile'],
    prompt: `# Simplify — 代码简化

在不改变外部行为的前提下，降低代码复杂度。目标是更少的代码、更清晰的结构、更低的维护成本。

## 简化原则

### 1. 不改变行为
- 外部可观察的输入输出必须完全一致
- 副作用（日志、状态变更、IPC 调用）必须保持
- 性能不能回退（除非用户明确同意取舍）

### 2. 最小改动
- 优先局部简化，避免大规模重构
- 一次只简化一个维度（命名、结构、重复、嵌套）
- 不引入新的抽象层，除非消除的重复足以支撑

## 简化维度

### A. 消除重复
- 识别重复的代码块（3 行以上相同逻辑）
- 提取为函数或常量
- 但：仅在重复出现 3 次以上时提取，避免过早抽象

### B. 简化条件
- 嵌套 if 转为提前返回（guard clause）
- 复杂三元表达式拆为独立变量
- 合并相同结果的分支

### C. 改善命名
- 变量名反映"是什么"而非"怎么做"
- 函数名用动词+名词
- 避免缩写（除非是领域通用术语）

### D. 减少状态
- 合并可合并的状态变量
- 移除从未使用的状态分支
- 把可计算的状态从存储改为派生

### E. 缩短作用域
- 变量声明靠近使用处
- 减少跨函数的可变状态
- 优先纯函数

## 执行流程

### Step 1 — 识别简化点
1. 读取目标文件
2. 标注每个简化机会（按维度分类）
3. 估算每个简化的收益与风险

### Step 2 — 与用户确认
向用户呈现简化清单：
- 每项标注：维度、收益、风险
- 推荐优先级：低风险高收益 → 高风险高收益 → 低收益
- 询问用户哪些要执行

### Step 3 — 逐步执行
1. 一次执行一个简化
2. 每次执行后验证（行为不变）
3. 记录改动，便于回滚

### Step 4 — 验证
1. 调用相关测试（若有）
2. 检查改动文件的语法
3. 向用户报告简化结果

## 约束
- 不要为简化而简化——每处改动都要有明确收益
- 不要在简化中夹带功能修改
- 不要重命名公共 API 或导出符号（影响外部调用方）
- 危险简化（删除看似未使用的代码）必须先确认确实无引用`
  },

  {
    id: 'bundled-verify',
    name: 'verify',
    description: '验证：在宣布任务完成前，系统化地检查所有需求是否满足、改动是否安全。',
    bundled: true,
    allowedTools: ['readFile', 'listDirectory', 'localSearch', 'runTerminalCommand'],
    prompt: `# Verify — 完成前验证

在向用户宣布"完成"之前，系统化地验证所有方面。宁可信其有 bug，不可信其无。

## 验证清单

### 1. 需求完整性
逐条对照用户的原始需求：
- [ ] 每个明确要求的功能都已实现
- [ ] 隐含的需求（错误处理、边界情况）已覆盖
- [ ] 没有引入未被要求的"附加功能"

### 2. 代码正确性
- [ ] 语法正确（无未闭合的括号、缺分号等）
- [ ] 引用的变量/函数都已定义
- [ ] 异步调用都有 await 或 .then 处理
- [ ] 资源（文件句柄、连接）都已释放
- [ ] 错误路径都有处理（try/catch、.catch）

### 3. 行为验证
- [ ] 正常路径已手动验证或测试
- [ ] 边界情况已考虑（空输入、超长输入、并发）
- [ ] 错误情况已考虑（网络失败、权限不足、磁盘满）

### 4. 影响范围
- [ ] 改动的文件都已列出
- [ ] 受影响的上下游模块已检查
- [ ] 没有意外的副作用（日志格式变化、UI 闪烁）

### 5. 安全与合规
- [ ] 没有引入硬编码的密钥或凭证
- [ ] 用户输入都已校验（防注入）
- [ ] 文件操作都限制了路径范围（防目录遍历）
- [ ] 没有引入不安全的依赖

### 6. 性能
- [ ] 没有引入 N+1 查询
- [ ] 大数据集操作有分页或流式处理
- [ ] 没有在热路径中引入不必要的同步操作

## 执行流程

### Step 1 — 列出验证项
根据任务类型，从清单中选取适用的项，生成具体验证任务。

### Step 2 — 逐项验证
1. 用工具验证（运行测试、搜索引用、读取文件）
2. 对每项记录结果：通过 / 失败 / 不适用
3. 失败项立即修复

### Step 3 — 修复后复验
1. 修复后重新验证相关项
2. 检查修复是否引入新问题
3. 重复直到全部通过

### Step 4 — 报告
向用户呈现验证报告：
- 验证项总数
- 通过/失败/不适用统计
- 已修复的问题
- 残留风险（若有）

## 约束
- 不要跳过验证直接宣布完成
- 不要把"应该工作"当作"已验证"
- 发现的问题要修复，不是隐瞒
- 残留风险必须明确告知用户`
  }
];

if (typeof module !== 'undefined' && module.exports) {
  module.exports = { BUNDLED_SKILLS };
}
